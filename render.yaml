services:
  - type: worker
    name: sonic-music-bot
    runtime: node
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      npm install
    startCommand: node index.js
    envVars:
      - key: NODE_VERSION
        value: 18
